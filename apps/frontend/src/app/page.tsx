import { StoryForm } from '@/components/features/story-form/StoryForm';
import { AppShell } from '@/components/layout/AppShell';

export default function HomePage() {
  return (
    <AppShell>
      <StoryForm />
    </AppShell>
  );
}
